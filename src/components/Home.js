import React from "react";
import AutoComplete from "./AutoComplete";

const Home = () => {
  return (
    <div className="flex flex-col m-4">
      <AutoComplete />
    </div>
  );
};

export default Home;
